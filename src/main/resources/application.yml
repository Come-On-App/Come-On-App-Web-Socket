spring:
  config:
    additional-location: config/
  profiles:
    group:
      local: "local,websocket,kafka,websocket-logging,admin"
      dev: "dev,websocket,kafka,websocket-logging,admin"
      prod: "prod,websocket,kafka,websocket-logging,admin"
    active: local
